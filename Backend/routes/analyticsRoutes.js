// const express = require("express");
// const { getAnalyticsData, getPaginatedAnalyticsData } = require("../controllers/analytics");

// const router = express.Router();

// // Route for basic analytics data (existing one)
// router.get("/", getAnalyticsData);

// // Route for paginated analytics data with search & filters
// router.get("/paginated", getPaginatedAnalyticsData);

// module.exports = router;
